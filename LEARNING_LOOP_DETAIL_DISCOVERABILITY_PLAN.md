# 知识构件详情可发现性改造方案

## 当前实施状态

- 已完成：双动作知识构件卡片、独立可滚动详情对话层、证据展开与空状态、详情到补学的单层切换。
- 真实预览反馈：逐构件独立操作行及目标摘要造成视觉冗余；现已将常驻“详情”入口收进节点右侧，保留 44px 点击区域，目标正文只在详情中展示，节点主体仍负责导航。
- 已完成：组件与学习闭环回归、TypeScript、ESLint、格式检查。
- 本地验收：用户已确认通过（2026-09-22），覆盖本轮提供的知识路径、详情与补学、提交验证、结课报告及刷新恢复体验清单。
- 待验证：指定的 1440×900、1280×720、390×844 尺寸走查记录，以及至少 5 位首次使用者的可发现性测试；本次本地验收未记录具体视口，不能代替这两项专项验收。
- 发布判断：代码可供本地预览，尚未满足扩大灰度所需的真实使用验证。

## 目标与边界

让用户在知识路径中无需猜测 `ⓘ` 图标的含义，就能发现“查看学习目标与理解证据”；同时保留点击构件切换课堂环节的既有行为。此项属于学习闭环 MVP 的可用性修正，不引入新的学习状态、证据规则或课程数据结构。

改造前，`components/stage/knowledge-path-sidebar.tsx` 中构件主体负责切换 Scene，详情使用右上角约 24px 的图标按钮与 320px Popover。当前改为常驻文字“详情”与独立对话层，继续保留主体导航。

## 产品决策

采用“卡片主体导航 + 显式详情操作 + 独立详情对话层”的双动作结构。

1. 展开知识路径时，每个知识构件节点右侧常驻文字“详情”，不依赖悬停或图标猜测；可访问名称说明其包含学习目标与理解证据。证据数量在详情内展示，节点不再增加独立操作行。
2. 卡片标题、类型和状态所在主体仍用于切换 Scene。详情操作是主体按钮的兄弟元素，不能形成嵌套按钮。点击详情不切换 Scene、播放位置或 Quiz 草稿。
3. 节点只展示标题、类型和状态，目标摘要统一收进详情，保持路径紧凑。`needs_revisit` 沿用节点状态文案“建议回看”，“开始补学”在详情中看到理由后触发。
4. 收起知识路径时保持现有节点导航；详情文字入口不压缩进 52px 栏。用户展开知识路径后即可看到明确入口，收起状态的节点 Tooltip 提示“展开路径可查看目标与证据”。
5. 详情改为独立的响应式对话层，宽度约 440–520px，高度不超过可视区域；主体滚动、标题与底部主操作保持可见。小屏宽度随视口收缩。使用现有 Radix Dialog 的焦点管理、Esc 关闭和焦点返回能力，不自制悬浮定位。
6. 详情内容顺序固定为：构件标题与状态 → 构建目标 → 关键点 → 证据数量与最近证据 → 下一步操作。无证据时显示明确空状态；证据较多时可展开历史，不让面板无限增高。
7. “这里没懂”或“开始补学”先关闭详情，再打开补学对话层；任一时刻只显示一个学习闭环对话层。切换 Scene、关闭侧栏或关闭功能开关时，详情也应关闭。

## 实施任务

### A. 卡片入口

- 在 `components/stage/knowledge-path-sidebar.tsx` 将原 24px 图标入口改为节点内常驻“详情”，保留至少 44px 的点击区域，并为导航内容预留右侧空间。
- 抽取明确的卡片操作区，保持 `data-testid="scene-item"` 仍指向导航按钮；新增 `data-testid="knowledge-component-details-trigger"` 用于详情按钮。
- 节点展示学习状态，证据数量和目标全文放入详情；不把“已浏览”或进入 Scene 误写成“已理解”。
- 功能开关关闭时沿用旧课堂节点，不渲染新的详情入口。

### B. 详情面板

- 新建 `components/learning-loop/component-details-dialog.tsx`，从 Sidebar 移出当前内联 Popover 内容。
- 使用受控 Dialog；由 Sidebar 持有 `selectedComponentId`，只允许一个详情实例打开。
- 详情展示最新状态和证据，而非打开时冻结的旧快照；证据更新后内容同步，但不会重复触发 `learning_loop_evidence_opened`。
- 关闭详情后焦点回到触发按钮；从详情进入补学时，焦点交给补学对话层。
- 保留“这里没懂”“开始补学”的原业务调用及错误提示，不更改 evidence/eventId、RepairPlan 或状态折叠逻辑。

### C. 回归与观测

- 组件测试：主体点击仅切换 Scene；详情点击仅打开详情；两者均可键盘操作；详情打开埋点只记一次。
- 状态测试：`not_started`、`in_progress`、`needs_revisit`、`verified` 的文案与主操作正确；无证据和长证据列表可正常显示。
- 交互测试：详情 → 这里没懂 → 补学时无双层叠加；关闭/切换 Scene 后无旧内容残留；Interactive iframe、Quiz 草稿和播放状态不被卸载。
- 视觉尺寸：至少检查 1440×900、1280×720、390×844，以及左栏收起/展开、助教展开/收起。详情标题、底部操作必须始终可见，主体可滚动，不遮挡关闭按钮。
- 埋点延用 `learning_loop_evidence_opened`，对比改造前后的详情打开率和“这里没懂”→补学启动率；埋点不参与学习证据折叠。

## 验收标准

1. 首次进入开启学习闭环的课堂，每个构件无需悬停即可看到“详情”；不再以 `ⓘ` 为唯一入口。
2. 点击主体始终导航，点击详情始终不导航；触控目标至少 44px 高，键盘 Tab 顺序清楚。
3. 在上述桌面与窄屏尺寸中，详情内容、关闭按钮和底部主操作可完整访问，不与助教、学习任务或实验 iframe 叠放错乱。
4. 详情与补学之间切换不会重复生成计划、丢失播放/Quiz/Interactive 状态，证据与状态刷新正确。
5. 学习闭环开关关闭时，既有课堂行为与界面不变。
6. 5 位首次使用者中至少 4 位能在 10 秒内自主找到构件详情；若未达到，先调整入口位置/文案，不扩大灰度。

## 交付顺序与风险

先完成卡片双动作结构，再替换详情面板，最后做多尺寸视觉与真实课堂回归。预计 1–2 个开发日，单独提交，不与 Phase 6 扩展项混合。

主要风险是卡片高度增加和 Sidebar 滚动变长；通过节点内短文字入口、目标和证据按需查看来控制。另一风险是对话层抢焦点；使用现有 Dialog 能力，并以键盘和详情→补学连续操作作为必测路径。
